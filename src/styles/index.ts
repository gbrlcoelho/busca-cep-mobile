import {normalize} from '@utils/index'

export const theme = {
  colors: {
    white: '#fff',
    background: '#282c34',
    yellow: '#f1c400',
  },
  fontSize: {
    h1: normalize(24),
    h2: normalize(16),
    h3: normalize(14),
    h4: normalize(12),
    h5: normalize(10),
    h6: normalize(8),
  },
}
